# 🎰 Casino Backoffice - Environment Variables (Multi-Brand)

# ======================================
# API Configuration
# ======================================
# Backend API URL - IMPORTANTE: Debe coincidir con donde corre tu backend
# 
# Opción 1 - Desarrollo Local con /etc/hosts (RECOMENDADO MULTI-BRAND):
# Configurar /etc/hosts primero:
#   127.0.0.1 sitea.local
#   127.0.0.1 siteb.local
# 
# Para SiteA:
# VITE_API_BASE_URL=http://api.sitea.local:5000/api/v1
# Acceder a: http://sitea.local:5173
#
# Para SiteB:
# VITE_API_BASE_URL=http://api.siteb.local:5000/api/v1
# Acceder a: http://siteb.local:5173
#
# Opción 2 - Desarrollo Local Simple (sin multi-brand):
VITE_API_BASE_URL=http://localhost:5000/api/v1
# Acceder a: http://localhost:5173
#
# Opción 3 - Producción (dominios específicos por brand):
# Para SiteA: VITE_API_BASE_URL=https://api.sitea.com/api/v1
# Para SiteB: VITE_API_BASE_URL=https://api.siteb.com/api/v1

# ======================================
# Development Settings
# ======================================
VITE_NODE_ENV=development

# Habilitar logging detallado de requests API en consola
VITE_ENABLE_API_LOGGING=true

# ======================================
# Multi-Brand Configuration
# ======================================
# IMPORTANTE: Con SameSite=Lax (nueva arquitectura):
# 
# ✅ Frontend y Backend DEBEN estar en el mismo dominio o subdominio
# ✅ Cookies son específicas por brand (sitea.com vs siteb.com)
# ✅ En desarrollo, usar /etc/hosts para brand resolution
# ✅ NO usar localhost para multi-brand - usar dominios locales (.local)
# 
# Ver más en: MULTI-BRAND-FRONTEND-GUIDE.md

# ======================================
# Notas Importantes para CORS
# ======================================
# ✅ El backend debe correr en el mismo protocolo (HTTP o HTTPS)
# ✅ withCredentials está habilitado para cookies HttpOnly
# ✅ SameSite=Lax requiere same-origin (no cross-origin)
# ✅ Backend valida brand en cada request
# ✅ NO incluir header 'Host' - el browser lo maneja automáticamente
# ✅ El backend debe tener el origin del frontend en la whitelist CORS